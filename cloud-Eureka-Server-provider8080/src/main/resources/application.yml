server:
  port: 8080

spring:
  application:
    name: CLOUD-PROVIDER-HYSTRIX-PAYMENT

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8004/eureka/


