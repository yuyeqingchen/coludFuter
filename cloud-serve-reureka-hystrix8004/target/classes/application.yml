server:
  port: 8004
spring:
  application:
    name: hystrix-Eureka-service


eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8004/eureka/
  instance:
    hostname: hystrixServer

